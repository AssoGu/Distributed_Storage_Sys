Init:
On proxy node:
1) call dss_app:start(), choose proxy in gui

On storage nodes:
1) call dss_app:start(), choose storage in gui, enter proxy node ip and capacity(currently in kb,need to be fixed)


Flow - upload/downloading from/to another storage node,

use menu.

to do-
2) Vnodes calculations formula

3)every node update his capacity after upload/download

3)dynamiclly added node - need to sort the ring.

4)if the same node rejoining the ring we do not add him again to the ring to avoid multiple vnodes (need to perform check in statisticsDB)

current 26/8 14:55
Program crashes when trying to exit a process.
